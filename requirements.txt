# Claude Tech Debt Analyzer Requirements

# This is a Claude Code skill - no external dependencies required!
# Claude analyzes the code directly using its native understanding.

# Optional: For YAML config support
pyyaml>=6.0

# Note: This skill requires Claude Code.
# It does NOT require API keys or external services.
# Claude (running the skill) analyzes your code directly.
